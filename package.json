{
  "name": "@janiscommerce/lambda",
  "version": "1.0.0",
  "description": "A package to handle JANIS Service internal Lambda invokes",
  "main": "lib/index.js",
  "scripts": {
    "test": "export TEST_ENV=true; mocha --exit -R nyan --recursive tests/",
    "test-ci": "nyc --reporter=html --reporter=text mocha --recursive tests/",
    "watch-test": "export TEST_ENV=true; mocha --exit -R nyan -w --recursive tests/",
    "coverage": "nyc npm test",
    "lint": "eslint lib/ tests/"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/janis-commerce/lambda.git"
  },
  "author": "Janis",
  "license": "ISC",
  "homepage": "https://github.com/janis-commerce/lambda.git#readme",
  "devDependencies": {
    "eslint": "^5.16.0",
    "eslint-config-airbnb-base": "^13.2.0",
    "eslint-plugin-import": "^2.18.2",
    "husky": "^2.4.1",
    "mocha": "^5.2.0",
    "mock-require": "^3.0.3",
    "nyc": "^13.1.0",
    "sinon": "^7.3.2"
  },
  "files": [
    "lib/"
  ],
  "directories": {
    "test": "tests"
  },
  "dependencies": {
    "@janiscommerce/api-session": "^1.3.1",
    "@janiscommerce/superstruct": "^1.1.1",
    "aws-sdk": "^2.672.0",
    "lllog": "^1.1.2"
  }
}
